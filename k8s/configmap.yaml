# =============================================================
# Telikos First Responder — Kubernetes ConfigMap
# Namespace: iom-preprod
#
# Non-secret values are set here directly.
# Secret values (marked REPLACE_WITH_KV_PATH) will be injected
# automatically by the cluster's Key Vault integration from ohpiom-nonprod-kv.
# Update the paths to match your vault secret names before deploying.
# =============================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: tfr-config
  namespace: iom-preprod
data:
  # ── Application config ──────────────────────────────────────
  USE_MOCK: "false"
  LOG_SOURCE: "grafana"
  K8S_NAMESPACE: "iom-preprod"
  K8S_CLUSTERS: "ohp-np-west-1,ohp-np-north-1,ohp-prod-west-1,ohp-prod-north-1"
  GRAFANA_URL: "https://grafana.maersk.io"
  GRAFANA_DATASOURCE_UID: "loki"
  LOOKBACK_SECONDS: "300"
  POLL_INTERVAL_SECONDS: "15"
  MAX_LINES_PER_POLL: "1000"
  LOKI_TIMEOUT_SECONDS: "60"
  LLM_PROVIDER: "openai"
  OPENAI_BASE_URL: "https://vibe-proxy.westeurope.dev.maersk.io/"
  OPENAI_API_VERSION: "2024-10-21"
  OPENAI_ANALYZE_MODEL: "claude-sonnet-4-6"
  OPENAI_CHAT_MODEL: "claude-sonnet-4-6"
  CORS_ORIGINS: "https://telikos-first-responder.sit.maersk-digital.net"
  SSO_ENABLED: "true"
  AZURE_AD_TENANT_ID: "05d75c05-fa1a-42e7-9cf1-eb416c396f2d"
  AZURE_AD_CLIENT_ID: "fdb5c5f4-d589-495b-8b84-c0f3abad2eb0"
  # ── Secrets (injected from ohpiom-nonprod-kv by cluster KV integration) ──
  GRAFANA_TOKEN: vault:ohpiom-nonprod-kv/data/readable/preprod/telikos-first-responder#grafanaToken
  OPENAI_API_KEY: vault:ohpiom-nonprod-kv/data/readable/preprod/telikos-first-responder#apiKey
  DATABASE_URL: vault:ohpiom-nonprod-kv/data/readable/preprod/telikos-first-responder#dbUrl
  ARM_CLIENT_ID: vault:ohpiom-nonprod-kv/data/readable/preprod/telikos-first-responder#clientId
  ARM_CLIENT_SECRET: vault:ohpiom-nonprod-kv/data/readable/preprod/telikos-first-responder#clientSecret
  ARM_TENENT_ID: vault:ohpiom-nonprod-kv/data/readable/preprod/telikos-first-responder#tenantId
